# TransFiler 制造业文档双语智能转换系统 - 更新日志 (Changelog)

本文档记录 **TransFiler** 各版本的更新内容、功能新增、问题修复与性能优化。

---

## 📌 [v1.1.2] - 2026-09-02

### 🧠 核心升级：全局配置与功能选项记忆引擎 (跨会话持久化)
1. **界面主题持久化记忆**：
   - 用户切换深色/浅色主题后，系统自动同步至本地缓存与数据库，软件关闭重启后无需重新切换，无缝保持上次使用的主题外观。
2. **大模型与 API 配置全局记忆**：
   - 完整持久化保存用户配置的 API Key、大模型服务商（DeepSeek / 硅基流动 / 阿里云等）、Base URL、模型名称及默认导出路径，退出软件绝不丢失。
3. **制作选项实时自动记忆**：
   - **源语言与目标语言**：记住上次选择或互换的语言方向（如英语 &rarr; 中文、中文 &rarr; 越南语等）；
   - **车间专业术语库绑定**：记住“启用”或“不启用”的勾选偏好；
   - **排版规则选项**：记住选中的排版模式（中文在上 / 中文在下 / 纯单语替换）；
   - 所有制作选项修改后即时自动持久化，再次打开软件即处于上次的工作状态。
4. **开机自动 API 连通性检测与实时状态指示**：
   - 每次打开软件或进入系统时，后台自动向配置的大模型 API 发起轻量级连通性检测握手；
   - 状态指示灯与状态标签实时显示当前在线/检测中/异常状态，支持一键点击前往排障。
5. **Excel (`.xlsx`) 复杂图文排版无损重构引擎 (OpenXML 级)**：
   - 彻底重写 Excel 解析与重构底层，采用 OpenXML 原生流式重构，100% 解决复杂制造 SOP/工艺文件（如作业指导书、排拉图）在传统转换中出现的**格式失真、图片被缩放拉伸、矢量图丢失**等问题；
   - 深度解析并提取 Drawing 绘图层中的所有**文本框说明（`<xdr:sp>` / `<xdr:txBody>`）**，将工艺图示中的关键作业规范一并纳入双语翻译，同时 100% 保留图片原始尺寸与锚点（`twoCellAnchor` / `oneCellAnchor`）；
6. **智能中文/多语种人名识别与免翻译保护**：
   - 内置制造业工程人名识别算法与常见百家姓形态分析，自动精准识别签署栏、编制人、审核人、批准人等人员姓名（如 `罗成耿`、`张三` 等）；
   - 人名直接保持原样不进行翻译，杜绝拼音乱译、音译错译或 Token 浪费。

---

## 📌 [v1.0.1] - 2026-08-31

### 🔧 核心修复与自检优化 (API 连通性增强)
1. **API Key 智能清洗与容错**：
   - 自动清除从网页/微信复制 API Key 时夹带的首尾空白、换行、零宽字符（如 `\u200b`、`\u3000`、`\ufeff` 等）；
   - 自动去除外层误带的单双引号（`"sk-..."` / `'sk-...'`）；
   - 自动过滤多余的 `Bearer ` 前缀，彻底避免因格式问题导致的 `401 Unauthorized` 鉴权失败。
2. **Base URL 规范化与防呆处理**：
   - 自动补齐缺失的协议头（`https://`）；
   - 自动去除末尾多余斜杠，以及用户误填的 `/chat/completions` 端点后缀，杜绝多层拼接导致的 `404 Not Found`。
3. **企业内网与代理网络自适应**：
   - 启用 `follow_redirects=True` 与 `trust_env=True`，保证系统代理（Clash/VPN/公司网关）正常生效；
   - 针对部分工厂局域网/杀毒软件开启 HTTPS 流量检查导致的 SSL 证书异常，增加安全自动降级重试机制。
4. **精准中文故障诊断与提示**：
   - **401** 明确提示：API Key 错误、未生效或模型提供商不匹配；
   - **402 / 429** 明确提示：账户余额不足 (Insufficient Balance) 或触发频率限制；
   - **404** 明确提示：Base URL 路径或模型名称不匹配；
   - **网络异常** 明确提示：连接超时或被防火墙拦截。
5. **模型兼容性降级 (JSON Mode)**：
   - 针对不支持 `response_format: {"type": "json_object"}` 的第三方模型或网关，自动降级为标准文本请求并智能提取 JSON，提升各类国内模型的适配能力。

### 🌐 新增功能与体验升级 (双向多语种互译 & 自定义排版规则)
1. **支持外语翻译为中文双语文档 (外文 &rarr; 中文)**：
   - 彻底打破“仅限中文转外文”的限制，源文档语言与目标翻译语言支持任意互选（支持中文、英语、越南语、泰语、印尼语、日语、韩语、西班牙语、德语、法语、俄语等 11 种语种）；
   - 新增 **一键语言互换快捷按钮**，支持快速在中译外与外译中之间切换；
   - 术语库智能双向索引：外译中时自动将外文专业词条精准回溯并强制翻译为标准中文术语。
2. **新增排版规则选项（中文在上 / 中文在下 / 纯替换）**：
   - **中文在上，外文在下 (`zh_top`)**：无论源文档是中文还是外文，均保证生成的双语版中文位于上方，外文位于下方；
   - **中文在下，外文在上 (`zh_bottom`)**：满足特殊海外工厂或合资企业标准，外文在上，中文注释在下；
   - **纯单语替换 (`replace`)**：直接将源文本覆盖为目标语言译文，保留 100% 原生格式。
3. **Word / Excel / CSV 全引擎排版升级**：
   - Word 段落与表格单元格重构引擎全面适配 `zh_top` 与 `zh_bottom` 排版；
   - Excel 自适应撑开行高算法根据双语各自字符权重动态计算最佳行高，杜绝文字溢出与遮挡。

### 🎨 界面排版优化与 Taste-Skill 工业级质感重构
1. **全栈接入 Taste-Skill 现代工学设计体系**：
   - 引入 `Geist` 现代极简无衬线体搭配 `JetBrains Mono` 等宽数据字体（开启 `tabular-nums`），提升工程数据与段落的可读性；
   - 优化双主题色彩系统（`Pro Dark` 曜石深灰 `#080b11` / `Studio Light` 纸感白 `#ffffff`），彻底消除刺眼纯黑/彩虹杂色，全站收敛为单一 Cobalt Blue 工业强调色；
   - 排版规则单选框升级为**三栏触感卡片 (Radio Cards)**，选中时平滑亮起强调色边框与微光底色，极具专业仪器质感；
   - 语言互换按钮增加平滑旋转微动效，按钮增加物理按压反馈（`active:scale-[0.985]`）；
2. **修复浅色模式下侧边栏对比度缺陷**：
   - 修复了浅色模式 (Studio Light) 下侧边栏背景误沿用深色变量导致的文字辨识度不足问题；
   - 浅色模式下侧边栏背景统一采用纯白 (`#ffffff`) 与柔和灰度边框 (`#e2e8f0`)，菜单文字呈现清晰的深深灰色 (`#475569` / `#0f172a`)，高对比度、清晰易读；
3. **严格保留经典第一版单列纵向流排版**：
   - 文档制作界面维持用户指定的经典单列纵向流式结构（顶部大面积触感拖拽区 &rarr; 紧凑文件预览卡片 &rarr; 规则面板 &rarr; 底部右侧主操作）；
   - 双栏校对工作台保留双向同步滚动（Synchronized Scroll）与即时加为术语功能。

---

## 🚀 [v1.0.0] - 2026-08-28 (首发正式版)

### 🌟 核心功能发布 (MVP)
1. **Word (`.docx`) 高保真双语排版引擎**：
   - XML 级解析独立段落与所有表格单元格；
   - 样式 100% 克隆继承（字体、字号、加粗、颜色、对齐方式），实现「中文在上、外文在下」双语排版；
   - 严格保留表格原有边框、合并单元格与背景底色（Shading）。
2. **Excel (`.xlsx`) 自适应行高双语引擎**：
   - 自动开启单元格 `wrap_text=True` 折行；
   - 创新性研发**自适应行高动态算法**（按中文字符 1.8 权重与英文字符 1.0 权重结合列宽计算），将行高由 24pt 自动撑开至 40~66pt，彻底杜绝双语文字被遮挡；
   - 单元格背景色与边框无损保留。
3. **CSV 表格与老版 `.doc` 兼容支持**：
   - CSV 自动检测 `utf-8-sig`、`gbk`、`gb2312` 编码并双语输出；
   - 兼容老版 Word 二进制 `.doc` 文件转换处理。
4. **制造业专属术语库与混合翻译流水线**：
   - 采用 Trie 树最长词优先匹配算法，防止复合工艺术语被拆分误译；
   - 构造制造业提示词约束注入大模型，强制锁定机台零部件与品质检验标准词；
   - 术语库支持 Excel/CSV 格式批量导入与导出、分类检索与增删改查。
5. **左右双栏对比校对工作台 (Diff & Proofreading)**：
   - 左侧展示中文原文（只读），右侧展示「上中下外」双语可编辑卡片；
   - 支持文字框失焦静默自动保存；
   - 渲染 `🏷️ 术语命中` 绿色徽章，支持点击「➕ 加为术语」一键将优质译文沉淀至词库。
6. **Windows 桌面端应用与历史记录**：
   - 基于 PyWebView 构建 1280x850 原生桌面窗口，若缺少环境自动秒级回退至默认浏览器；
   - 记录历史制作文件，支持一键在 Windows 资源管理器中高亮定位导出文件。
